<div>
  <%= form_with model: [commentable, comment] do |form| %>
    <div class="field">
      <%= form.text_area :content, required: true %>
    </div>
    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>
</div>
