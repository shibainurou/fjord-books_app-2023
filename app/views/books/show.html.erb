<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t("link.edit"), edit_book_path(locale: I18n.locale, id: @book) %> |
  <%= link_to t("link.back"), books_path %>

  <%= button_to t("show.button.destroy"), book_path(locale: I18n.locale, id: @book), method: :delete %>
</nav>
