<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t("link.edit", name: Book.model_name.human), edit_book_path(locale: I18n.locale, id: @book) %> |
  <%= link_to t("link.back", name: Book.model_name.human(count: 2)), books_path %>

  <%= button_to t("show.button.destroy", name: Book.model_name.human), book_path(locale: I18n.locale, id: @book), method: :delete %>
</nav>
