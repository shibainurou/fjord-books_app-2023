<h1><%= t("edit.title") %></h1>

<%= render "form", book: @book %>

<br>

<nav class="page-nav">
  <%= link_to t("link.show"), edit_book_path(locale: I18n.locale, id: @book) %> |
  <%= link_to t("link.back"), books_path %>
</nav>
