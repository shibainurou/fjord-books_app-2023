<h1><%= t('edit.title', name: Book.model_name.human) %></h1>

<%= render "form", book: @book %>

<br>

<nav class="page-nav">
  <%= link_to t("link.show", name: Book.model_name.human), edit_book_path(locale: I18n.locale, id: @book) %> |
  <%= link_to t("link.back", name: Book.model_name.human(count: 2)), books_path %>
</nav>
