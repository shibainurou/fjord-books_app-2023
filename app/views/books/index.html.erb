<div class="top-menu">
  <%= link_to t('views.common.title_index', name: i18n_pluralize(User.model_name.human)), users_path %>
  <%= link_to @current_user.email, edit_user_registration_path %>
  <%= link_to t('views.common.sing_out'), destroy_user_session_path, data: { turbo_method: :delete } %>
</div>

<h1><%= t('views.common.title_index', name: i18n_pluralize(Book.model_name.human)) %></h1>

<div id="books" class="index-items">
  <% @books.each do |book| %>
    <div class="index-item">
      <%= render book %>
      <p>
        <%= link_to t('views.common.show', name: Book.model_name.human.downcase), book %>
      </p>
    </div>
  <% end %>
</div>

<%= paginate @books %>

<nav class="page-nav">
    <%= link_to t('views.common.new', name: Book.model_name.human.downcase), new_book_path %>
</nav>
